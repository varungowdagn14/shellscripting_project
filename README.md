Assignment:

	1.	Check nginx, zip is installed
→ if yes
	•	then check status of nginx
	•	if nginx is active, host website
	•	else, start nginx & host website
→ if no
	•	then install nginx, zip
	•	start nginx
	•	host website

Note: take url & path from user to host website
